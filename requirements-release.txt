#
# Pinned for RELEASE BUILDS ONLY.
#
# Developers install with `pip install -e ".[gui,web,dev]"` and get the
# flexible floors from pyproject.toml. This file exists so that a binary built
# from a tag six months from now is the same binary, rather than whatever
# happened to be newest on PyPI that morning.
#
# Universal: it carries environment markers rather than being resolved for one
# platform, because the same file feeds the Windows, macOS and Linux builds.
#
# Regenerate with:
#   uv pip compile --universal --extra gui --extra web \
#     --output-file requirements-release.txt pyproject.toml
#
# This file was autogenerated by uv via the following command:
#    uv pip compile --universal --extra gui --extra web --output-file requirements-release.txt pyproject.toml
blinker==1.9.0
    # via flask
click==8.4.2
    # via flask
colorama==0.4.6 ; sys_platform == 'win32'
    # via click
et-xmlfile==2.0.0
    # via openpyxl
flask==3.1.3
    # via kmz-points (pyproject.toml)
itsdangerous==2.2.0
    # via flask
jinja2==3.1.6
    # via flask
lxml==6.1.1
    # via kmz-points (pyproject.toml)
markupsafe==3.0.3
    # via
    #   flask
    #   jinja2
    #   werkzeug
mgrs==1.5.4
    # via kmz-points (pyproject.toml)
openpyxl==3.1.5
    # via kmz-points (pyproject.toml)
packaging==26.3
    # via kmz-points (pyproject.toml)
tkinterdnd2==0.6.2
    # via kmz-points (pyproject.toml)
utm==0.9.0
    # via kmz-points (pyproject.toml)
waitress==3.0.2
    # via kmz-points (pyproject.toml)
werkzeug==3.1.8
    # via flask
